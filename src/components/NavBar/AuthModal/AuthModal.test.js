import { render, screen } from '@testing-library/react';
import AuthModal from '.';

test('should render email input', () => {
  render(<AuthModal />);
  // const inputElement = screen.getAllByRole('input');
});
